<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="fragments.SorteiosFragment">

    <!-- TODO: Update blank fragment layout -->
    <TextView
        android:id="@+id/TextViewMensagemTopoID"
        android:layout_width="0dp"
        android:layout_height="46dp"
        android:layout_marginTop="57dp"
        android:background="@drawable/fundo_degrade_blue"
        android:paddingTop="8dp"
        android:text="Carregando saldo..."
        android:textAlignment="center"
        android:textColor="@color/branco_padrao"
        android:textSize="18sp"
        android:textStyle="bold"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ProgressBar
        android:id="@+id/progressBarSorteioID"
        style="?android:attr/progressBarStyle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginBottom="248dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/TextViewMensagemTopoID"
        app:layout_constraintVertical_bias="0.981" />

    <ListView
        android:id="@+id/listViewSorteiosID"
        android:layout_width="0dp"
        android:layout_height="448dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:divider="@android:color/darker_gray"
        android:dividerHeight="1dp"
        app:layout_constraintTop_toBottomOf="@+id/TextViewMensagemTopoID" />

</android.support.constraint.ConstraintLayout>
